= form_with(url: sessions_path, scope: :session, method: :post) do |f|
  %section
    =f.label :email, "Email: "
    =f.email_field :email
  %section
    =f.label :password, "Password: "
    =f.password_field :password

  = submit_tag("Login")